rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // This application stores data locally; cloud access is disabled.
    match /{document=**} { allow read, write: if false; }
  }
}
